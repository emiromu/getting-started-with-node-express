<html>
    <head>
        <link rel="stylesheet" href="/styles.css">
    </head>      
    <body>
        <%= locals.message %>
        <%- include('navbar', {links: links}) %>
        <ul>
            <% users.forEach((user) => { %>
            <%- include('user', {user: user}); %>
            <% }); %>
        </ul>      
    </body>
</html>
